# Contributing Guidelines

1. Fork the repo
2. Create a new branch (`git checkout -b feature/your-idea`)
3. Commit with clear messages
4. Push and open a PR

Keep code clean, readable, and tested. Don’t push random `__pycache__` junk.
